node_modules/
dist/
*.tsbuildinfo
.tmp/
.DS_Store
*.local

# Local dogfood built from the private game repo (real, git-crypt content).
# Kept out of the framework repo; replaced by a clean starter in Phase 4.
examples/university/

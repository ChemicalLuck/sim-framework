/// <reference types="vite/client" />

// The engine's Vite plugins generate `virtual:*` modules at build time. The
// engine ships their ambient declarations at `@chemicalluck/sim-engine/vite-env`.
/// <reference types="@chemicalluck/sim-engine/vite-env" />
